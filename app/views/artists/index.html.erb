<h1>hello index of artists</h1>
<ol>
<% @artists.each do |artist| %><br>
    <li>
    <%= link_to artist.name, artist_path(artist) %><br>
      (<%= artist.song_count %> <%= "song".pluralize(artist.song_count) %>)
    </li>
<% end %>
</ol>