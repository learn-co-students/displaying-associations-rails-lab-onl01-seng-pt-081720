<h1>Songs' Index Page</h1><br>


<ul>
  <% @songs.each do |song| %>
    <li>
       Artist Name:
          <%= link_to song.artist_name, song_path(song.artist) %>
          -
          Song Title:
          <%=  link_to song.title, song_path(song) %>
  
    </li>
  <% end %>
</ul>